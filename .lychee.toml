# Lychee link checker configuration
# Checks internal links in built output: npm run linkcheck

# Don't check email addresses
exclude_mail = true

# Exclude false positives from intentional content examples
exclude = [
  "evil",
]
